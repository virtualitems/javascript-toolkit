if (! window) {
  throw new Error(1);
}


function isAnimationEvent(target) {

  if (! window.AnimationEvent) {
    throw new Error(2);
  }

  return (target instanceof window.AnimationEvent);
}


function isAnimationPlaybackEvent(target) {

  if (! window.AnimationPlaybackEvent) {
    throw new Error(2);
  }

  return (target instanceof window.AnimationPlaybackEvent);
}


function isAudioProcessingEvent(target) {

  if (! window.AudioProcessingEvent) {
    throw new Error(2);
  }

  return (target instanceof window.AudioProcessingEvent);
}


function isBeforeInstallPromptEvent(target) {

  if (! window.BeforeInstallPromptEvent) {
    throw new Error(2);
  }

  return (target instanceof window.BeforeInstallPromptEvent);
}


function isBeforeUnloadEvent(target) {

  if (! window.BeforeUnloadEvent) {
    throw new Error(2);
  }

  return (target instanceof window.BeforeUnloadEvent);
}


function isBlobEvent(target) {

  if (! window.BlobEvent) {
    throw new Error(2);
  }

  return (target instanceof window.BlobEvent);
}


function isCharacterBoundsUpdateEvent(target) {

  if (! window.CharacterBoundsUpdateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.CharacterBoundsUpdateEvent);
}


function isClipboardEvent(target) {

  if (! window.ClipboardEvent) {
    throw new Error(2);
  }

  return (target instanceof window.ClipboardEvent);
}


function isCloseEvent(target) {

  if (! window.CloseEvent) {
    throw new Error(2);
  }

  return (target instanceof window.CloseEvent);
}


function isCompositionEvent(target) {

  if (! window.CompositionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.CompositionEvent);
}


function isContentVisibilityAutoStateChangeEvent(target) {

  if (! window.ContentVisibilityAutoStateChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.ContentVisibilityAutoStateChangeEvent);
}


function isCookieChangeEvent(target) {

  if (! window.CookieChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.CookieChangeEvent);
}


function isCustomEvent(target) {

  if (! window.CustomEvent) {
    throw new Error(2);
  }

  return (target instanceof window.CustomEvent);
}


function isDeviceMotionEvent(target) {

  if (! window.DeviceMotionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.DeviceMotionEvent);
}


function isDeviceOrientationEvent(target) {

  if (! window.DeviceOrientationEvent) {
    throw new Error(2);
  }

  return (target instanceof window.DeviceOrientationEvent);
}


function isDocumentPictureInPictureEvent(target) {

  if (! window.DocumentPictureInPictureEvent) {
    throw new Error(2);
  }

  return (target instanceof window.DocumentPictureInPictureEvent);
}


function isDragEvent(target) {

  if (! window.DragEvent) {
    throw new Error(2);
  }

  return (target instanceof window.DragEvent);
}


function isErrorEvent(target) {

  if (! window.ErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.ErrorEvent);
}


function isFocusEvent(target) {

  if (! window.FocusEvent) {
    throw new Error(2);
  }

  return (target instanceof window.FocusEvent);
}


function isFontFaceSetLoadEvent(target) {

  if (! window.FontFaceSetLoadEvent) {
    throw new Error(2);
  }

  return (target instanceof window.FontFaceSetLoadEvent);
}


function isFormDataEvent(target) {

  if (! window.FormDataEvent) {
    throw new Error(2);
  }

  return (target instanceof window.FormDataEvent);
}


function isGPUUncapturedErrorEvent(target) {

  if (! window.GPUUncapturedErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.GPUUncapturedErrorEvent);
}


function isGamepadEvent(target) {

  if (! window.GamepadEvent) {
    throw new Error(2);
  }

  return (target instanceof window.GamepadEvent);
}


function isHIDConnectionEvent(target) {

  if (! window.HIDConnectionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.HIDConnectionEvent);
}


function isHIDInputReportEvent(target) {

  if (! window.HIDInputReportEvent) {
    throw new Error(2);
  }

  return (target instanceof window.HIDInputReportEvent);
}


function isHashChangeEvent(target) {

  if (! window.HashChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.HashChangeEvent);
}


function isIDBVersionChangeEvent(target) {

  if (! window.IDBVersionChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.IDBVersionChangeEvent);
}


function isInputEvent(target) {

  if (! window.InputEvent) {
    throw new Error(2);
  }

  return (target instanceof window.InputEvent);
}


function isKeyboardEvent(target) {

  if (! window.KeyboardEvent) {
    throw new Error(2);
  }

  return (target instanceof window.KeyboardEvent);
}


function isMIDIConnectionEvent(target) {

  if (! window.MIDIConnectionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MIDIConnectionEvent);
}


function isMIDIMessageEvent(target) {

  if (! window.MIDIMessageEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MIDIMessageEvent);
}


function isMediaEncryptedEvent(target) {

  if (! window.MediaEncryptedEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MediaEncryptedEvent);
}


function isMediaKeyMessageEvent(target) {

  if (! window.MediaKeyMessageEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MediaKeyMessageEvent);
}


function isMediaQueryListEvent(target) {

  if (! window.MediaQueryListEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MediaQueryListEvent);
}


function isMediaStreamEvent(target) {

  if (! window.MediaStreamEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MediaStreamEvent);
}


function isMediaStreamTrackEvent(target) {

  if (! window.MediaStreamTrackEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MediaStreamTrackEvent);
}


function isMessageEvent(target) {

  if (! window.MessageEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MessageEvent);
}


function isMouseEvent(target) {

  if (! window.MouseEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MouseEvent);
}


function isMutationEvent(target) {

  if (! window.MutationEvent) {
    throw new Error(2);
  }

  return (target instanceof window.MutationEvent);
}


function isNavigateEvent(target) {

  if (! window.NavigateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.NavigateEvent);
}


function isNavigationCurrentEntryChangeEvent(target) {

  if (! window.NavigationCurrentEntryChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.NavigationCurrentEntryChangeEvent);
}


function isOfflineAudioCompletionEvent(target) {

  if (! window.OfflineAudioCompletionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.OfflineAudioCompletionEvent);
}


function isPageRevealEvent(target) {

  if (! window.PageRevealEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PageRevealEvent);
}


function isPageSwapEvent(target) {

  if (! window.PageSwapEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PageSwapEvent);
}


function isPageTransitionEvent(target) {

  if (! window.PageTransitionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PageTransitionEvent);
}


function isPaymentMethodChangeEvent(target) {

  if (! window.PaymentMethodChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PaymentMethodChangeEvent);
}


function isPaymentRequestUpdateEvent(target) {

  if (! window.PaymentRequestUpdateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PaymentRequestUpdateEvent);
}


function isPictureInPictureEvent(target) {

  if (! window.PictureInPictureEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PictureInPictureEvent);
}


function isPointerEvent(target) {

  if (! window.PointerEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PointerEvent);
}


function isPopStateEvent(target) {

  if (! window.PopStateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PopStateEvent);
}


function isPresentationConnectionAvailableEvent(target) {

  if (! window.PresentationConnectionAvailableEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PresentationConnectionAvailableEvent);
}


function isPresentationConnectionCloseEvent(target) {

  if (! window.PresentationConnectionCloseEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PresentationConnectionCloseEvent);
}


function isProgressEvent(target) {

  if (! window.ProgressEvent) {
    throw new Error(2);
  }

  return (target instanceof window.ProgressEvent);
}


function isPromiseRejectionEvent(target) {

  if (! window.PromiseRejectionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.PromiseRejectionEvent);
}


function isRTCDTMFToneChangeEvent(target) {

  if (! window.RTCDTMFToneChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCDTMFToneChangeEvent);
}


function isRTCDataChannelEvent(target) {

  if (! window.RTCDataChannelEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCDataChannelEvent);
}


function isRTCErrorEvent(target) {

  if (! window.RTCErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCErrorEvent);
}


function isRTCPeerConnectionIceErrorEvent(target) {

  if (! window.RTCPeerConnectionIceErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCPeerConnectionIceErrorEvent);
}


function isRTCPeerConnectionIceEvent(target) {

  if (! window.RTCPeerConnectionIceEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCPeerConnectionIceEvent);
}


function isRTCTrackEvent(target) {

  if (! window.RTCTrackEvent) {
    throw new Error(2);
  }

  return (target instanceof window.RTCTrackEvent);
}


function isSecurityPolicyViolationEvent(target) {

  if (! window.SecurityPolicyViolationEvent) {
    throw new Error(2);
  }

  return (target instanceof window.SecurityPolicyViolationEvent);
}


function isSensorErrorEvent(target) {

  if (! window.SensorErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.SensorErrorEvent);
}


function isSpeechSynthesisErrorEvent(target) {

  if (! window.SpeechSynthesisErrorEvent) {
    throw new Error(2);
  }

  return (target instanceof window.SpeechSynthesisErrorEvent);
}


function isSpeechSynthesisEvent(target) {

  if (! window.SpeechSynthesisEvent) {
    throw new Error(2);
  }

  return (target instanceof window.SpeechSynthesisEvent);
}


function isStorageEvent(target) {

  if (! window.StorageEvent) {
    throw new Error(2);
  }

  return (target instanceof window.StorageEvent);
}


function isSubmitEvent(target) {

  if (! window.SubmitEvent) {
    throw new Error(2);
  }

  return (target instanceof window.SubmitEvent);
}


function isTaskPriorityChangeEvent(target) {

  if (! window.TaskPriorityChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TaskPriorityChangeEvent);
}


function isTextEvent(target) {

  if (! window.TextEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TextEvent);
}


function isTextFormatUpdateEvent(target) {

  if (! window.TextFormatUpdateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TextFormatUpdateEvent);
}


function isTextUpdateEvent(target) {

  if (! window.TextUpdateEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TextUpdateEvent);
}


function isToggleEvent(target) {

  if (! window.ToggleEvent) {
    throw new Error(2);
  }

  return (target instanceof window.ToggleEvent);
}


function isTouchEvent(target) {

  if (! window.TouchEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TouchEvent);
}


function isTrackEvent(target) {

  if (! window.TrackEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TrackEvent);
}


function isTransitionEvent(target) {

  if (! window.TransitionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.TransitionEvent);
}


function isUIEvent(target) {

  if (! window.UIEvent) {
    throw new Error(2);
  }

  return (target instanceof window.UIEvent);
}


function isUSBConnectionEvent(target) {

  if (! window.USBConnectionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.USBConnectionEvent);
}


function isVirtualKeyboardGeometryChangeEvent(target) {

  if (! window.VirtualKeyboardGeometryChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.VirtualKeyboardGeometryChangeEvent);
}


function isWebGLContextEvent(target) {

  if (! window.WebGLContextEvent) {
    throw new Error(2);
  }

  return (target instanceof window.WebGLContextEvent);
}


function isWheelEvent(target) {

  if (! window.WheelEvent) {
    throw new Error(2);
  }

  return (target instanceof window.WheelEvent);
}


function isWindowControlsOverlayGeometryChangeEvent(target) {

  if (! window.WindowControlsOverlayGeometryChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.WindowControlsOverlayGeometryChangeEvent);
}


function isXRInputSourceEvent(target) {

  if (! window.XRInputSourceEvent) {
    throw new Error(2);
  }

  return (target instanceof window.XRInputSourceEvent);
}


function isXRInputSourcesChangeEvent(target) {

  if (! window.XRInputSourcesChangeEvent) {
    throw new Error(2);
  }

  return (target instanceof window.XRInputSourcesChangeEvent);
}


function isXRReferenceSpaceEvent(target) {

  if (! window.XRReferenceSpaceEvent) {
    throw new Error(2);
  }

  return (target instanceof window.XRReferenceSpaceEvent);
}


function isXRSessionEvent(target) {

  if (! window.XRSessionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.XRSessionEvent);
}


function iswebkitSpeechRecognitionEvent(target) {

  if (! window.webkitSpeechRecognitionEvent) {
    throw new Error(2);
  }

  return (target instanceof window.webkitSpeechRecognitionEvent);
}

